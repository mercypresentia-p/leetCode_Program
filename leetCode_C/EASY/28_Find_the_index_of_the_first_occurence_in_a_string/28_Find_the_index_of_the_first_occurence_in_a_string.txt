int strStr(char* haystack, char* needle) {
    int n = strlen(needle);
    for (int i = 0; haystack[i]; i++) {
        int j;
        for (j = 0; j < n; j++) {
            if (haystack[i + j] != needle[j])
                break;
        }
        if (j == n)
            return i;
    }
    return -1;
}
